#include "lightship/api.h"
#include "@NAME@/services.h"

void register_services(struct plugin_t* plugin, struct lightship_api_t* api)
{
    /* -----------------------------------------------------
    * All services this plugin supports
    * ---------------------------------------------------*/
    /*
     * api->service_register(plugin, "service_name_1", service_callback_1);
     * api->service_register(plugin, "service_name_2", service_callback_2);
     * etc...
     */

}
